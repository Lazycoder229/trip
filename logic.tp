# logical.tp
let x = 50

if x > 0 and x < 100
    outn("in range")

let done      = false
let cancelled = true

if done or cancelled
    outn("finished")

let active = false

if !active
    outn("inactive")