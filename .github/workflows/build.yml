name: Build Trip

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    name: Build Windows Installer
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install pkg
        run: npm install -g pkg

      - name: Build trip.exe
        run: pkg trip.js --targets node18-win-x64 --output trip.exe --icon trip-icon.ico

      - name: Install NSIS
        run: choco install nsis -y

      - name: Build TripSetup.exe installer
        run: |
          $env:PATH += ";C:\Program Files (x86)\NSIS"
          makensis installer.nsi
        shell: powershell

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: trip-windows-installer
          path: TripSetup.exe

  build-linux:
    name: Build Linux Binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install pkg
        run: npm install -g pkg

      - name: Build trip-linux
        run: pkg trip.js --targets node18-linux-x64 --output trip-linux

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: trip-linux
          path: trip-linux

  release:
    name: Create Release
    needs: [build-windows, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Download Windows installer
        uses: actions/download-artifact@v4
        with:
          name: trip-windows-installer

      - name: Download Linux binary
        uses: actions/download-artifact@v4
        with:
          name: trip-linux

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            TripSetup.exe
            trip-linux
          generate_release_notes: true